// Main javascript entry point
// Should handle bootstrapping/starting application

'use strict';

var $ = require('jquery');

/*
$(function() {
  var $form    = $(document.getElementById("register")),
      form_email = $(document.getElementById("form-EMAIL")),
      MCE_email = $(document.getElementById("mce-EMAIL")),
      MCE_form = $(document.getElementById("mc-embedded-subscribe-form"));

  $form.on("submit", function() {
    MCE_email.val($form_email.val());
    MCE_form.submit()
    return false;
  });
});

*/